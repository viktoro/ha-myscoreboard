[project]
name = "ha-myscoreboard"
version = "0.1.0"
requires-python = ">=3.12"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "aiohttp",
    "async-timeout",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
